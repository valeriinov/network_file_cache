name: network_file_cache
description: Core CacheManager utilities for network_file_cache.
version: 0.0.1
repository: https://github.com/valeriinov/network_file_cache

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
