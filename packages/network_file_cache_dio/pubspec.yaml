name: network_file_cache_dio
description: Dio adapter for network_file_cache.
version: 0.0.1
repository: https://github.com/valeriinov/network_file_cache
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.0
  network_file_cache:
    git:
      url: https://github.com/valeriinov/network_file_cache
      ref: 0.0.1
      path: packages/network_file_cache

dev_dependencies:
  flutter_test:
    sdk: flutter
